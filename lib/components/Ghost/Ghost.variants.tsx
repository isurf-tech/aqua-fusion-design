import { tv } from 'tailwind-variants'

const ghostVariants = tv({
    base: 'py-2 px-4 hover:bg-neutral-light rounded-l-full rounded-r-full font-inter text-xs font-semibold focus:outline-none focus:outline-offset-2 focus:outline-2 focus:outline-brand-medium',
})

export default ghostVariants
